# انتشار عمومی سایت

سایت محلی تا وقتی روی میزبان اینترنتی منتشر نشده باشد، فقط از دستگاه خودت یا شبکهٔ خانگی قابل‌دسترسی است. برای انتشار، API و فایل‌های سایت با هم در یک سرویس Docker اجرا می‌شوند؛ داده‌ها باید در PostgreSQL ابری بمانند، نه در فایل H2 داخل کانتینر.

## مراحل انتشار

1. مخزن پروژه را در GitHub قرار بده.
2. یک دیتابیس PostgreSQL در Supabase بساز و رشتهٔ اتصال JDBC را از بخش اتصال دیتابیس بردار.
3. در Render یک Blueprint جدید بساز و مخزن را وصل کن؛ فایل `render.yaml` سرویس Docker را تعریف می‌کند و کلید مدیر تصادفی تولید می‌شود.
4. هنگام ساخت Blueprint این متغیرها را وارد کن:
   - `DB_URL`: رشتهٔ اتصال JDBC از Supabase، با `sslmode=require`.
   - `DB_USER`: نام کاربری دیتابیس.
   - `DB_PASSWORD`: رمز دیتابیس.
   - `STORE_ADMIN_KEY`: یک رمز تصادفی و طولانی که فقط مدیر بداند.
5. سرویس را Deploy کن. Render برای سرویس یک نشانی عمومی `onrender.com` می‌دهد؛ همان را برای مشتری‌ها بفرست.

برنامهٔ JavaFX مدیر را با نشانی API عمومی و همان کلید مدیریت اجرا کن. در PowerShell:

```powershell
$env:STORE_API = "https://YOUR-SERVICE.onrender.com/api/products"
$env:STORE_ADMIN_KEY = "YOUR-ADMIN-KEY"
mvn -pl admin-app javafx:run
```

برای اجرای محلی، مقدار کلید پیش‌فرض `local-dev-key` است. کلید واقعی را در مخزن یا پیام عمومی قرار نده. در Render از PostgreSQL استفاده کن؛ فایل‌سیستم سرویس رایگان ماندگار نیست و ممکن است سرویس پس از ۱۵ دقیقه بی‌کاری متوقف شود و درخواست بعدی با تأخیر بالا بیاید. پلن رایگان PostgreSQL در Render هم پس از ۳۰ روز منقضی می‌شود، برای همین دیتابیس جداگانهٔ Supabase در این راهنما استفاده شده است.

این نسخه هنوز سفارش واقعی، پرداخت، حساب کاربری مشتری و احراز هویت کامل مدیر ندارد؛ در حال حاضر سایت فقط کالاها را نمایش می‌دهد و کلید API از افزودن/ویرایش/حذف عمومی کالا جلوگیری می‌کند.
